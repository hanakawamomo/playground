import { IDisposable } from '@lumino/disposable';
import { DocumentRegistry } from '@jupyterlab/docregistry';
import { NotebookPanel, INotebookModel } from '@jupyterlab/notebook';
import { JupyterFrontEnd, JupyterFrontEndPlugin } from '@jupyterlab/application';
import { CommandRegistry } from '@lumino/commands';
/**
 * A notebook widget extension that adds Dbx specific functionality
 */
export declare class CustomDbxExtension implements DocumentRegistry.IWidgetExtension<NotebookPanel, INotebookModel> {
    _commands: CommandRegistry;
    constructor(commands: CommandRegistry);
    /**
     * Create a new extension object.
     */
    createNew(panel: NotebookPanel, context: DocumentRegistry.IContext<INotebookModel>): IDisposable;
}
declare const _default: JupyterFrontEndPlugin<void, JupyterFrontEnd.IShell, "desktop" | "mobile">[];
/**
* Export the plugin as default.
*/
export default _default;
