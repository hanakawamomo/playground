import { ReactWidget } from '@jupyterlab/apputils';
import { Notebook, NotebookPanel } from '@jupyterlab/notebook';
import * as React from 'react';
import { Widget } from '@lumino/widgets';
export declare function createMagicSwitcher(panel: NotebookPanel): Widget;
/**
 * A toolbar widget that switches magic used in the cell.
 */
export declare class MagicSwitcher extends ReactWidget {
    /**
     * Construct a new cell type switcher.
     */
    constructor(widget: Notebook);
    /**
     * Handle `change` events for the HTMLSelect component.
     */
    handleChange: (event: React.ChangeEvent<HTMLSelectElement>) => void;
    /**
     * Handle `keydown` events for the HTMLSelect component.
     */
    handleKeyDown: (event: React.KeyboardEvent) => void;
    render(): JSX.Element;
    private _notebook;
}
